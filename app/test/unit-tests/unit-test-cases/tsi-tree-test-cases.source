#!/bin/bash

script_name="$(basename ${BASH_SOURCE})"
cur_test_num="0"

(( ++cur_test_num ))
(tsi_tree "--name" > "/dev/null")
handle_test_exit_status \
    "${invalid_command_args_count_err_exit_status}" \
    "${?}" \
    "${cur_test_num}" \
    "${script_name}"
